PORT=8080
JWT_SECRET=change_me_in_production
DB_PATH=./chatroom.db
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# reCAPTCHA v2 secret key — must match the site key above.
# Get it from https://www.google.com/recaptcha/admin (same key pair).
# When set, registration requests are verified against Google's API.
# Leave blank to skip verification (development without a key).
RECAPTCHA_SECRET_KEY=
